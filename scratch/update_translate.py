import fs from 'fs';
import path from 'path';

// Original `content/translate_json.js` file is completely replaced here to incorporate robust translation of vyanjan (consonants).
// Note: This file will rewrite `translate_json.js` by providing the full updated script.
// Wait, I will just rewrite the `translate_json.js` directly using standard javascript rather than writing a python script to do it.
